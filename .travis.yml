language: cpp
install:
  - ./travis-diagnostics.sh
  - sudo add-apt-repository --yes ppa:ubuntu-toolchain-r/test
  - sudo apt-get update
  - sudo apt-get install gcc-4.7
  - ./travis-diagnostics.sh
script: "./gradlew --no-color -q describe test"
